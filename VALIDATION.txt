OpenCalc v1.1.10 buildfix129 validation

PASS: build.sh is valid Bash syntax.
PASS: a clean-checkout simulation invoked git clone --depth 1 for https://github.com/NullMagic2/Rust-HLP-Viewer, built both programs, and installed executable OpenCalc and hlp-viewer files side by side.
PASS: an existing-checkout simulation reused Rust-HLP-Viewer/viewer/Cargo.toml and completed with a deliberately unusable git command, proving that no clone is attempted when the project is already present.
PASS: the viewer package is built from viewer/Cargo.toml into Rust-HLP-Viewer/target/release/hlp-viewer.
PASS: Linux OpenCalc resolves the companion as current_exe().parent()/hlp-viewer and launches it with the selected localized HLP file as its first argument.
PASS: the supplied README is present at the project root as README.md and matches the uploaded file byte for byte.
PASS: build-linux/OpenCalc.cfg remains preserved across rebuilds.
PASS: Rust-HLP-Viewer, target, and build-linux are excluded from the source manifest and source archive.
PASS: SOURCE_MANIFEST.sha256 and ZIP integrity were regenerated and verified.

The two build-flow checks used deterministic stand-in git, cargo, and pkg-config commands so that clone/reuse, output placement, permissions, and packaging could be exercised without network access or the unavailable Rust/GTK/wxWidgets toolchain. A real network clone, native compilation, and GUI launch were therefore not performed in this container.

Buildfix130 Linux HLP-viewer manifest recovery
- build.sh no longer assumes the repository's current root Cargo.toml is valid.
- New clones retain Git history instead of using a depth-one checkout.
- Existing depth-one checkouts are unshallowed only when recovery is necessary.
- When the root workspace manifest is malformed, build.sh temporarily restores the newest Cargo-valid Cargo.toml from that repository's own history.
- The original upstream Cargo.toml is restored immediately after the viewer build and also by an EXIT trap after failure or interruption.
- No hard-coded replacement workspace manifest is maintained in OpenCalc.
