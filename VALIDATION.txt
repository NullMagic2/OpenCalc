v1.1.10 buildfix88

Original executable audit
-------------------------
Reference: CALC(1).EXE
Size: 59,392 bytes
SHA-256: b064b0ac430264eff7b79b91e743bcd36d7b3707857f5bcdc4db146911dd0e28

PASS: decoded Calculator RT_STRING resources 60..78 and recovered the five math/display errors exactly:
  67  Cannot divide by zero.
  68  Invalid input for function.
  69  Result of function is undefined.
  70  Result is too large.
  71  Result is too small.
PASS: recovered integration/startup errors:
  73  Cannot open Clipboard.
  74  There is not enough memory for data.\rClose one or more programs, and then try again.
  78  Not Enough Memory
PASS: central math-error dispatcher identified at 0x00404B13; 28 direct calls to it were enumerated in the disassembly.
PASS: _matherr-style adapter at 0x00404B5A maps DOMAIN=1 -> invalid input, OVERFLOW=3 -> too large, UNDERFLOW=4 -> too small, and other math exceptions -> undefined.
PASS: internal-error adapter at 0x00404B7F maps 0x83 -> divide by zero, 0x84 -> too large, 0x85 -> too small, and other internal errors -> undefined.
PASS: explicit call-site checks include negative sqrt/non-positive ln/log -> undefined; inverse-function domains -> invalid; tan abs(result)>1e15 -> undefined; square/cube/factorial overflow -> too large; reciprocal/div/mod zero -> divide by zero; empty Ave -> divide by zero; non-decimal signed range errors -> too large/too small.
PASS: unary Not has its own out-of-DWORD guard and reports invalid input (0x00404399..0x004043AE), while binary And/Or/Xor/Lsh report too large for the equivalent range violation (0x00404D31..0x00404D59).
PASS: Inv+x^y was traced at 0x00404FCE..0x0040501F: Inv is consumed, y==0 reports invalid input at 0x00404FFE, otherwise the operation is x^(1/y).
PASS: OpenClipboard failure path was identified around 0x00403CED..0x00403D0B and uses resource 73.
PASS: failed WinHelpA path was identified around 0x00403ED7..0x00403EF1 and uses the resource-74 data-memory message.
PASS: startup 0x400-byte allocation failure was identified around 0x00401093..0x004010ED and uses resource 78.
PASS: ASCII/UTF-16 string scan found additional Microsoft C runtime fatal strings (DOMAIN/SING/TLOSS/runtime/OOM); these are not Calculator-owned RT_STRING UI messages and are intentionally not exposed as ordinary calculator errors.

Reimplementation/source checks
------------------------------
PASS: all eight recovered Calculator-owned error strings are centralized in src/errors.rs.
PASS: the previous generalized "Result is undefined or too large." diagnostic is absent from the Rust sources.
PASS: sqrt, ln/log, inverse trig/hyperbolic, tangent-asymptote, factorial, reciprocal, arithmetic, power/underflow, Not, binary-logic, base-conversion, statistics, and inverse-power paths now select the recovered error categories rather than collapsing them together.
PASS: an internal BinaryOp::Root preserves the original Inv+x^y behavior in both button-driven scientific input and the scientific expression buffer.
PASS: Ave with an empty Statistics Box now enters the divide-by-zero error state; standard deviation with zero or one sample still returns zero.
PASS: memory-add now uses the recovered addition overflow classification.
PASS: Windows OpenClipboard failure uses the recovered clipboard message. Modern Rust-owned clipboard-buffer failures use the recovered data-memory diagnostic as the nearest app-owned equivalent; REVERSE_ENGINEERING.md documents that this is an adaptation rather than an identical original call site.
PASS: failed external HLP-viewer launch after both files are resolved maps to the original failed-WinHelp data-memory diagnostic.
PASS: startup performs a fallible 0x400-byte reserve so the original startup-memory message has a corresponding recoverable path.
PASS: every recovered source error key has English, Portuguese, and Spanish runtime-message coverage.
PASS: localization regression coverage iterates all eight recovered strings in all three languages.
PASS: source-level regression tests were added for the recovered math categories, tangent guard, base-conversion sign split, statistics behavior, binary overflow guards, inverse power/root zero, and memory-add overflow.
PASS: UTF-8 and balanced (), [], {} scan completed across every Rust source file while ignoring comments/string/char/raw literals.
PASS: BinaryOp::Root is wired through calc.rs, ui/mod.rs help mapping, and expr.rs parsing.

Buildfix78 platform-separated frontend architecture
---------------------------------------------------
PASS: the obsolete mixed-platform src/ui.rs, src/platform.rs, and src/locale.rs files are absent.
PASS: src/ui/mod.rs contains shared calculator construction, actions, History, Graph, and Statistics behavior; it selects exactly one backend module and contains no target-specific widget or OS API implementation.
PASS: src/ui/windows.rs contains wxMSW-only theming, decorator construction, selector notification bridging, native-message routing, DPI fallback, and Statistics focus policy.
PASS: src/ui/linux.rs contains wxGTK-only surface/font policy, sash-safe History separator construction, logical pane sizing, synchronous modal dialogs, and Calculator-retained Statistics focus.
PASS: src/ui/other.rs provides the portable fallback without importing Windows or GTK APIs.
PASS: src/platform/mod.rs exposes one shared integration facade and shared localized Help-file selection; Win32/USER32/GDI code is confined to src/platform/windows.rs, GTK3 code is confined to src/platform/linux.rs, and portable stubs are confined to src/platform/other.rs.
PASS: Linux native Help-viewer executable-bit validation remains covered by a Linux-backend unit test; selected-language Help fallback tests remain in the shared platform module.
PASS: src/locale/mod.rs contains canonical/display conversion only; Windows NLS, glibc LC_NUMERIC, and invariant fallback discovery are isolated in separate backend files.
PASS: Windows, Linux, and fallback UI backends expose the same 17-function frontend facade.
PASS: Windows, Linux, and fallback platform backends expose the same public integration function set.
PASS: tools/check_platform_separation.py accepts the tree and verifies backend facade parity, target-neutral shared modules, and absence of the obsolete mixed source files.
PASS: tools/check_rust_delimiters.py accepts all 22 Rust source files.
PASS: bash -n accepts build.sh and clean-linux.sh; every Python helper compiles with py_compile.
PASS: the refactor intentionally preserves the existing buildfix77 behavior; no calculator arithmetic, parser, graph model, persistence format, Help payload, or Windows visual policy was changed.

Runtime/toolchain limitation
----------------------------
This environment does not contain cargo, rustc, rustfmt, rust-analyzer, or a Windows runtime, so no Rust compilation or live Windows smoke test is claimed. The supplied build.bat/test.bat remain the authoritative compilation and unit-test path on a Rust-enabled Windows system.


Buildfix33 pasted-expression extension
--------------------------------------
PASS: lexer maps `**` to the same power token as `^`, while a single `*` remains multiplication.
PASS: power stays right-associative and accepts arbitrary expression exponents through the existing `powf` path.
PASS: regression cases cover 2**2, 5**2, 2**3, 2**-3, 9**0.5, 4**1.5, 2**(1+2), 2**3**2, and legacy 2^3.
PASS: `pi` and `pi()` both resolve to the same constant; `sqrt()`, `sin()`, `cos()`, and `tan()` continue through the existing function evaluator and current angle mode.
PASS: pasted factorial syntax covers postfix `!`, parenthesized expressions, `factorial(x)`, and `fact(x)`.
PASS: factorial precedence is above exponentiation (`2^3!` evaluates as 2^(3!)).
PASS: factorial preserves recovered Win95 error categories: negative/fractional -> Invalid input for function; >170 -> Result is too large.
PASS: UTF-8 and delimiter/static consistency scans rerun after the buildfix33 changes.

Runtime/toolchain limitation is unchanged: cargo/rustc/rustfmt are not installed in this environment, so no local Rust compilation is claimed.


Buildfix34 undo/redo extension
-----------------------------
PASS: Edit menu contains localized Undo (Ctrl+Z) and Redo (Ctrl+Y) entries before Copy/Paste.
PASS: wxDragon menu items are retained as handles and enable/disable from history availability.
PASS: all undoable calculator operations pass through a snapshot boundary; unchanged/no-op input does not create history.
PASS: new successful mutations clear the redo branch after Undo.
PASS: pasted expressions, numeric/operator/function input, memory operations, Standard/Scientific mode, radix, angle, Inv/Hyp, F-E, and statistics mutations are included in history.
PASS: Statistics Box LOAD, CD, and CAD mutations use the same history path.
PASS: restoring a snapshot synchronizes Standard/Scientific panel visibility, menu radio checks, selectors, display, memory/parens indicators, and statistics list.
PASS: decimal separator is deliberately not an undoable calculator state preference; restored snapshots are normalized to the currently persisted separator.
PASS: English/Portuguese/Spanish Undo and Redo labels/help strings are centralized in src/i18n.rs.
PASS: src/history.rs regression tests cover ordered undo/redo, redo invalidation after a new branch, and the bounded history limit.
PASS: the history module caps the undo stack at 256 calculator snapshots.

Runtime/toolchain limitation is unchanged: cargo/rustc/rustfmt are not installed in this environment, so no local Rust compilation is claimed.


Buildfix35 calculation-history feature + buildfix36 architecture correction (historical)
--------------------------------------------------------------------------------------
HISTORICAL NOTE: the separate History companion architecture described in this section was used by buildfix36-39 and is superseded by buildfix40's in-frame splitter.
PASS: History still provides a localized heading, read-only multiline right-aligned history control with native vertical scrolling, and a bottom-aligned classic Clear button.
PASS: View contains a localized checkable History item; toggling it now shows/hides only the companion window and never changes Calculator client width.
PASS: History visibility is persisted through OpenCalc.cfg as history_visible=true|false and defaults to visible when the setting is absent.
PASS: the visible calculation log is independent from Ctrl+Z/Ctrl+Y snapshot history; clearing the log never clears Undo/Redo, and Undo/Redo does not rewrite performed-operation history.
PASS: completed Standard equals/chaining operations, Scientific equals expressions, unary functions, percentages, statistics aggregate commands, and pasted expressions are logged with their resulting display/error.
PASS: calculation entries are newest-first and bounded to 256 records; logging continues while History is hidden.
PASS: History menu/title/Clear labels and History menu-help text are centralized for English, Portuguese, and Spanish.
PASS: src/calculation_log.rs regression tests cover newest-first ordering, bounding, and clearing.

Buildfix36 detached History companion checks (historical; superseded by buildfix40)
------------------------------------------------------------------------------------
PASS: src/ui/mod.rs no longer contains the buildfix35 `surface_panel`/combined-width architecture.
PASS: Calculator startup width is STD_W unconditionally; HISTORY_W is never added to the Calculator frame width.
PASS: Standard and Scientific Calculator panels are once again direct children of the Calculator frame, matching the pre-History buildfix34 architecture.
PASS: History is created as a separate wxDragon Frame with Calculator as its owner/parent and without normal caption/taskbar chrome.
PASS: Standard and Scientific History child panels live inside the companion frame, not inside either Calculator panel.
PASS: sync_mode_surface fits only the active Calculator panel to STD_W×STD_H or SCI_W×SCI_H; History visibility has no effect on those dimensions.
PASS: View > History only updates the persisted checked state and calls companion show/hide positioning; it does not invoke Calculator resizing.
PASS: main-frame move and size events re-snap the companion; mode switches and Undo/Redo mode restoration also resynchronize it.
PASS: Windows companion placement uses GetWindowRect + GetClientRect + GetDpiForWindow and positions only the History HWND/panel HWND with SetWindowPos.
PASS: buildfix37 aligns History's outer top/bottom edges with Calculator's outer window and keeps its left edge at Calculator's outer right edge.
PASS: History title/text/Clear controls are re-laid out to the realized companion client height so the scroll area grows and Clear remains bottom-aligned after outer-height docking.
PASS (superseded by buildfix39): buildfix36-38 explicitly hid History on owner minimization; buildfix39 preserves logical visibility instead so native owner restoration cannot be lost.

Buildfix37 companion alignment + History recall checks (historical positioning)
------------------------------------------------------------------------------
PASS: CalculationLogEntry stores both rendered result text and an optional exact f64 recall value.
PASS: error results are logged with value=None; numeric completed operations capture calc.value() without reparsing localized display text.
PASS: the rendered newest-first History text builds UTF-16 character ranges that map each visible entry back to its CalculationLog record.
PASS: on Windows, History click hit-testing uses EM_CHARFROMPOS plus EM_LINEINDEX/EM_POSFROMCHAR so wrapping and scrolling are reflected in the character position and empty space below the final line is rejected.
PASS: clicking a numeric History entry calls Calculator::recall_history_value and restores the captured result without re-evaluating the original expression or appending a duplicate calculation-log item.
PASS: History recall is routed through mutate_calculator, so Ctrl+Z can restore the pre-recall calculator state.
PASS: Calculator::recall_history_value clears pending/error/scientific-entry state while preserving mode, radix/angle configuration, memory, statistics data, and formatting preferences.
PASS: unit coverage verifies a recalled result can immediately become the left operand of a new binary calculation.
PASS: Statistics Box is created with Calculator as its parent/owner and is positioned from realized outer HWND rectangles.
PASS (buildfix37 historical behavior, superseded by buildfix38): Statistics Box originally docked to Calculator's left; buildfix38 replaces that placement with owner-relative centering.
PASS: main-frame move, size, and activation events re-synchronize both History and Statistics companions.

Runtime/toolchain limitation is unchanged: cargo/rustc/rustfmt and a Windows runtime are not installed in this environment, so no local Rust compilation or live Windows smoke test is claimed.


Buildfix38 startup + Statistics centering checks (historical History placement)
-----------------------------------------------------------------------------
PASS: Calculator is centered only after the native DPI fitter has established the final Standard/Scientific outer HWND size.
PASS: Windows startup centering uses MonitorFromWindow(MONITOR_DEFAULTTONEAREST) + GetMonitorInfoW.rcWork, so the final Calculator outer rectangle is centered in the nearest monitor's usable work area rather than across the virtual desktop/taskbar.
PASS: wxDragon Frame::centre remains the fallback when native Windows centering is unavailable.
PASS: History positioning still runs after Calculator centering and remains glued to Calculator's right outer edge; Calculator width/layout is not altered.
PASS: Statistics is positioned before its first show call, preventing a default-position flash.
PASS: Statistics uses the realized Calculator and Statistics outer rectangles to center the utility window over its owner.
PASS: Statistics centering is clamped to the owner's monitor work area and is re-applied on Calculator move/size/activation events.
PASS: the previous left-docking fallback (`main_pos.x - stats_size.width`) is absent.

Runtime/toolchain limitation is unchanged: cargo/rustc/rustfmt and a Windows runtime are not installed in this environment, so no local Rust compilation or live Windows smoke test is claimed.


Buildfix39 minimize/restore companion-state checks (historical History sidecar)
-------------------------------------------------------------------------------
PASS: sync_history_companion no longer calls History.show(false) merely because Calculator is iconized; explicit View > History=false remains the only persistent History hide path.
PASS: sync_stats_box no longer calls Statistics.show(false) merely because Calculator is iconized; an existing StatsBox therefore remains logically open across owner minimization.
PASS: Ui tracks whether a real minimize transition occurred with main_was_minimized, preventing ordinary WM_SIZE/restyle events from being mistaken for restore operations.
PASS: Windows platform integration installs a dedicated SetWindowSubclass observer for WM_SIZE and distinguishes SIZE_MINIMIZED from SIZE_RESTORED/SIZE_MAXIMIZED.
PASS: a genuine restore calls restore_open_companions, which derives visibility from application state rather than transient native visibility: history_visible controls History and stats_box=Some controls Statistics.
PASS: hidden History remains hidden after restore; closed/never-created Statistics remains absent because sync_stats_box is inert when stats_box=None.
PASS: existing wx move, size, and activation handlers remain as synchronization fallbacks and continue to re-position visible companions.
PASS: the native state notifier removes its subclass and drops its callback binding on WM_NCDESTROY.

Runtime/toolchain limitation is unchanged: cargo/rustc/rustfmt and a Windows runtime are not installed in this environment, so no local Rust compilation or live Windows smoke test is claimed.


Buildfix40 native splitter checks
---------------------------------
PASS: History is no longer a separate top-level Frame. src/ui/mod.rs creates one Calculator Frame containing one wxDragon SplitterWindow, with calculator_host as the left child and history_panel.panel as the right child.
PASS: the splitter uses SplitterWindowStyle::Vertical so Calculator and History are true adjacent panes inside one client area; the obsolete Win32 position_history_companion helper and all History sidecar HWND positioning code are absent.
PASS: LiveUpdate is intentionally omitted. The native splitter therefore presents the classic drag guide while moving the sash; Calculator controls are not continuously clipped/reflowed during a drag.
PASS: Standard and Scientific panels remain fixed-layout children of calculator_host at their recovered coordinates. Only the active Calculator panel is explicitly sized after a split/mode change.
PASS: View > History now uses split_vertically/unsplit on the existing History panel and resizes the single Calculator frame; it never creates, moves, hides, or restores a second History top-level window.
PASS: the programmatic splitter-adjusting guard prevents SetSashPosition-generated events from being interpreted as user width changes.
PASS: a user sash release is converted into a History-width delta, clamped to 120..420 source pixels; the outer Calculator frame grows/shrinks and the sash is normalized back to the exact Standard/Scientific Calculator boundary.
PASS: OpenCalc.cfg now persists history_width in addition to history_visible; settings tests cover parsing, serialization, and low/high clamping.
PASS: splitter sash and pane sizes use the existing HWND-aware DPI metric path on Windows, while the portable fallback remains logical-coordinate based.
PASS: History title, scrolling text area, and Clear button are re-laid out from the realized History-pane size after sash, DPI, and Standard/Scientific height changes.
PASS: clickable History recall remains attached to the single History text control and still restores the stored numeric result without re-evaluation or duplicate logging.
PASS: History is now a child of Calculator, so minimize/restore visibility follows its parent naturally. Explicit View > History=false remains represented by the unsplit state and is not changed by restore handling.
PASS: Statistics remains the only owned top-level companion. It is re-centered after Calculator mode changes, History show/hide, and History-width changes.
PASS: startup centering is performed after sync_mode_surface establishes the final combined Calculator+History frame width, so the single split application is centered as a unit.
PASS: UTF-8, delimiter balance, stale-symbol, settings-wiring, splitter-wiring, and archive/manifest validation were rerun after the buildfix40 changes.

Runtime/toolchain limitation is unchanged: cargo/rustc/rustfmt and a Windows runtime are not installed in this environment, so no local Rust compilation or live Windows smoke test is claimed.


Buildfix42 classic 3-D rendering audit
--------------------------------------
PASS: original CALC.EXE import table contains DrawFrameControl and DrawEdge.
PASS: disassembly at 0x004025E0..0x00402642 confirms DFC_BUTTON=4 with normal state 0x10 and pressed state 0x210.
PASS: disassembly at 0x00402804 confirms EDGE_ETCHED=0x06 with BF_BOTTOM=0x02.
PASS: original edge table at 0x0040C3C8 contains EDGE_ETCHED/BF_RECT (0x06/0x0f) and EDGE_SUNKEN/BF_RECT (0x0a/0x0f).
PASS: Rust Windows painter now calls DrawFrameControl for buttons, DrawEdge EDGE_ETCHED for group/separator frames, and DrawEdge EDGE_SUNKEN for display/status wells.
PASS: no recovered Calculator control coordinates or History splitter geometry were changed by this visual-only pass.


Buildfix43 Linux classic-rendering audit
----------------------------------------
PASS: the Windows DrawFrameControl/DrawEdge painter path is unchanged.
PASS: Linux no longer leaves install_classic_button/display/sunken-field/group/separator painter hooks as no-ops.
PASS: Linux uses a single process-lifetime GTK3 CSS provider attached to the underlying wxGTK widgets; this keeps native widget labels, interaction, pressed/disabled state and accessibility intact.
PASS: classic palette is fixed to Win95 defaults: face #c0c0c0, highlight #ffffff, light #dfdfdf, shadow #808080 and dark shadow #000000.
PASS: normal buttons use raised outer/inner edges; :active buttons invert those edges and offset content by one pixel.
PASS: displays/status wells use recessed two-stage edges, group boxes use etched edges, and the Scientific separator is a two-line shadow/highlight rule.
PASS: Linux main/splitter/Calculator/History/Statistics surfaces use the same #c0c0c0 face, avoiding GTK-theme background mismatch.
PASS: History's multiline field receives the Linux-only recessed-field CSS; Windows multiline History rendering is deliberately not routed through the single-line USER32 display painter.
PASS: Statistics RET/LOAD/CD/CAD buttons now use the same classic button hook as Calculator/History buttons on both supported painter paths.
PASS: GTK3 FFI is cfg-gated to target_os=linux; macOS and other non-Windows targets do not link libgtk-3.
PASS: no recovered control coordinates, splitter dimensions, calculation behavior or configuration schema changed.

Runtime/toolchain limitation: this environment still does not contain cargo/rustc/rustfmt or a Linux wxGTK runtime, so no native compilation/runtime smoke test is claimed.


Buildfix44 Statistics application-focus audit
---------------------------------------------
PASS: Calculator and Statistics activation handlers now drive one explicit companion-application-active state.
PASS: Windows uses WM_NCACTIVATE on the Statistics top-level HWND so its caption remains visually active when Calculator is clicked; no SetFocus/SetForegroundWindow call is used.
PASS: real keyboard focus remains with the window/control the user clicked, preserving calculator typed input, menu accelerators and Ctrl+Z/Ctrl+Y.
PASS: transitions Calculator -> Statistics and Statistics -> Calculator briefly pass through inactive/active events but end with Statistics in the active visual state.
PASS: transitions from either Calculator or Statistics to another application end with the Statistics companion in the inactive state.
PASS: closed or never-created Statistics remains inert because the UI helper returns immediately when stats_box=None.
PASS: Linux uses gtk_window_set_keep_above only while Calculator/Statistics is active and removes the hint on application deactivation; it does not force GTK keyboard focus.
PASS: History remains a child splitter pane and is unaffected by this top-level companion-focus policy.
PASS: no calculator state, expression parser, Undo/Redo history, persistent settings, History log, splitter geometry or Statistics dataset behavior changed.

Buildfix45 Windows compilation fix
----------------------------------
- Declared USER32 InvalidateRect in the Windows FFI block; buildfix44 called it
  from the classic display painter without declaring it, causing E0425.
- Gated the wxDragon StaticBox import to non-Windows targets because it is only
  used by the Linux/other-platform classic-frame fallback. This removes the
  Windows unused-import warning without disabling the Linux implementation.
Buildfix46 Statistics activation guard
--------------------------------------
PASS: Statistics installs a native companion activation subclass with Calculator as its owner reference.
PASS: WM_NCACTIVATE(FALSE) is rewritten to active only when its documented lParam identifies Calculator as the window being activated.
PASS: switching to another application is not intercepted and therefore paints Statistics inactive normally.
PASS: the subclass removes itself on WM_NCDESTROY, avoiding a stale owner/companion association after Statistics closes.
PASS: keyboard focus is not forcibly redirected, preserving Calculator input, accelerators and RET behavior.

Buildfix47 visible History separator
------------------------------------
PASS: History remains the right child of the existing wxDragon SplitterWindow; no top-level sidecar or inter-window gap was reintroduced.
PASS: a dedicated two-pixel decorative StaticText is anchored at History x=0 and resized to the full realized pane height.
PASS: Windows paints the rule with DrawEdge(EDGE_ETCHED, BF_LEFT) over COLOR_BTNFACE and returns HTTRANSPARENT so it cannot interfere with splitter dragging.
PASS: Linux maps the same painter hook to a GTK3 two-pixel shadow/highlight vertical rule.
PASS: History content margins, persisted history_width, Calculator Standard/Scientific widths, sash normalization, and calculation/history behavior are unchanged.
PASS: source ZIP integrity, complete SHA-256 source manifest, buildfix46 -> buildfix47 patch, UTF-8 and delimiter/static scans were rerun after this visual-only change.

Runtime/toolchain limitation is unchanged: cargo/rustc/rustfmt and a Windows runtime are not installed in this environment, so no local Rust compilation or live Windows smoke test is claimed.


Buildfix48 stronger button 3-D bevel
-----------------------------------
PASS: Windows custom pushbutton painting no longer relies on the subtler single `DrawFrameControl` bevel; it now paints an explicit two-level Win95-style highlight/shadow frame plus a stronger lower-right outer shadow.
PASS: the unpressed button text is confined to the lifted face area so the stronger dark lower/right edge remains visible.
PASS: pressed buttons still invert the relief and keep the one-pixel pressed text offset.
PASS: Linux wxGTK classic-button CSS now adds a stronger outer lower-right shadow while preserving the previous inset active state.
PASS: button labels, colours, sizes, event wiring, and calculator/history geometry are unchanged by this chrome-only adjustment.
PASS: source ZIP integrity, complete SHA-256 source manifest, buildfix47 -> buildfix48 patch, and basic static scans were rerun after this visual-only change.


Buildfix49 joined History separator lines
----------------------------------------
PASS: the decorative History separator is now a child of the shared wxSplitterWindow rather than the History pane interior.
PASS: the separator is positioned on the sash boundary, so the vertical etched rule visually joins the surrounding horizontal etched lines instead of starting after the sash gap.
PASS: the separator remains hidden whenever History is hidden and visible whenever History is shown.
PASS: Windows mouse hit-testing remains transparent for the decorative rule, so splitter dragging is unaffected.
PASS: History child layout, persisted history_width, Calculator pane geometry, and splitter normalization behavior are unchanged.
PASS: source ZIP integrity, complete SHA-256 source manifest, buildfix48 -> buildfix49 patch, and basic static scans were rerun after this visual-only adjustment.


Buildfix50 DPI-scaled Windows button relief
-------------------------------------------
PASS: the Windows pushbutton bevel no longer uses literal one- and two-device-pixel offsets; all edge/drop dimensions are derived from GetDpiForWindow using 96-DPI source metrics.
PASS: at 200% DPI the two classic bevel levels are two physical pixels each and the lower-right projection is four physical pixels.
PASS: the raised face is geometrically shortened before the lower/right shadow is painted, giving the shadow its own region instead of drawing every strip within the face.
PASS: button chrome now uses fixed classic RGB values C0C0C0, FFFFFF, DFDFDF, 808080, and 000000 instead of current Windows theme 3-D colours.
PASS: five solid brushes are cached for process lifetime rather than allocated during every WM_PAINT.
PASS: pressed buttons fill the full client, invert the edge, remove the projected drop, and apply a DPI-scaled label displacement.
PASS: enabled/disabled label handling, coloured labels, wxButton command events, control sizes, History layout, and Calculator geometry are unchanged.
PASS: source ZIP integrity, complete SHA-256 source manifest, buildfix49 -> buildfix50 patch, and static source checks were rerun after the painter correction.


Buildfix51 OpenCalc/resource/Linux Help/Statistics checks
---------------------------------------------------------
PASS: Cargo package identity is `opencalc`, the explicit binary target is `OpenCalc`, and build.bat publishes `OpenCalc.exe`; the localized main-frame title still comes exclusively from `Strings::calculator_title`.
PASS: build.rs invokes winresource only for a Windows target and links calc95.ico plus OpenCalc PE version strings; the existing WM_SETICON path remains for live frame icons.
PASS: OpenCalc.cfg is the primary settings path and the former Calculator95-Rust.cfg remains a read-only migration candidate when the renamed file is absent.
PASS: Windows HLP discovery still selects hlp-viewer.exe; Linux requires an extensionless native hlp-viewer beside the running OpenCalc binary and no longer attempts to execute the bundled Windows PE viewer.
PASS: Linux/native-viewer missing diagnostics are localized in English, Portuguese, and Spanish.
PASS: while Statistics is the Windows foreground window, Calculator handles WM_MOUSEACTIVATE with MA_NOACTIVATE; the mouse message is not discarded, so Calculator controls remain clickable without stealing actual focus from Statistics.
PASS: the owner-side guard checks GetForegroundWindow()==Statistics, so clicking OpenCalc from another application still activates the application normally rather than leaving it in the background.
PASS: RET explicitly raises/focuses Calculator and therefore remains the intentional way to transfer real focus out of Statistics.
PASS: the owner subclass is removed when either Statistics or Calculator is destroyed, avoiding a stale companion HWND after Statistics closes and reopens.
PASS: the Statistics wx activation fallback remains compiled only outside Windows for the GTK keep-above policy; Windows caption state now follows real activation instead of synthetic WM_NCACTIVATE messages.
PASS: source ZIP integrity, complete SHA-256 source manifest, buildfix50 -> buildfix51 patch, UTF-8/static scans, and fresh-extraction manifest verification were rerun.

Runtime/toolchain limitation is unchanged: cargo/rustc/rustfmt, a Windows resource compiler, and a Windows runtime are not installed in this environment, so no local Rust compile, PE-resource inspection, or live activation smoke test is claimed.


Buildfix52 native-face/DPI-border checks
-----------------------------------------
PASS: the Windows button face no longer uses fixed RGB C0C0C0; both face painting and opaque text background use the active COLOR_BTNFACE value.
PASS: the former separately reserved lower/right drop region is removed, so no oversized black projection consumes or darkens the button client area.
PASS: raised buttons use two bevel layers derived from GetDpiForWindow: highlight/light on top-left and shadow/dark-shadow on bottom-right.
PASS: at 200% DPI each one-source-pixel layer is two physical pixels and the complete bevel is four physical pixels, while the face colour remains unchanged.
PASS: pressed buttons invert the same two DPI-scaled layers and retain a DPI-scaled label offset.
PASS: all bevel colours come from Windows system 3-D colour slots rather than a hard-coded face palette.
PASS: button labels, command events, control rectangles, History geometry, OpenCalc identity/resources, Linux Help selection, and Statistics activation code are unchanged.
PASS: source ZIP integrity, complete SHA-256 source manifest, buildfix51 -> buildfix52 patch, and fresh-extraction verification were rerun.

Runtime/toolchain limitation is unchanged: cargo/rustc/rustfmt, a Windows resource compiler, and a Windows runtime are not installed in this environment, so no local Rust compile, PE-resource inspection, or live Windows visual smoke test is claimed.


Buildfix53 History-separator alignment checks
----------------------------------------------
PASS: the decorative History rule is centred over the native sash instead of occupying only the Calculator side of the boundary.
PASS: the rightward adjustment is half of the DPI-scaled separator width, yielding approximately one device pixel at 100% and two device pixels at 200%.
PASS: the actual sash position, persisted history_width, Calculator/History pane dimensions, and drag behaviour are unchanged.
PASS: mouse transparency and History show/hide behaviour remain unchanged.
PASS: source ZIP integrity, complete SHA-256 source manifest, buildfix52 -> buildfix53 patch, and fresh-extraction verification were rerun.


Buildfix54 History decimal-separator synchronization checks
------------------------------------------------------------
PASS: calculation-log expressions and numeric results are normalized to invariant `.` punctuation when recorded.
PASS: History rendering localizes canonical numeric punctuation using the currently selected period/comma preference on every refresh.
PASS: set_decimal_separator explicitly refreshes the existing History pane after updating Calculator and keypad labels.
PASS: changing comma -> period -> comma does not cumulatively rewrite or lose numeric punctuation because the stored log remains canonical.
PASS: decimal replacement requires an ASCII digit on both sides; function argument punctuation, prose, and localized error-message periods remain unchanged.
PASS: nonnumeric/error entries retain their original runtime-message key and continue to follow language changes independently of decimal punctuation.
PASS: exact f64 History recall values, entry order/ranges, log limits, clearing, and Calculator arithmetic are unchanged.
PASS: source ZIP integrity, complete SHA-256 source manifest, buildfix53 -> buildfix54 patch, and fresh-extraction verification were rerun.


Buildfix55 native Statistics ownership/activation checks
---------------------------------------------------------
PASS (static): Statistics is explicitly assigned Calculator as its native Win32 owner through GWLP_HWNDPARENT; no global WS_EX_TOPMOST style is introduced.
PASS (static): the WM_MOUSEACTIVATE/MA_NOACTIVATE subclass is installed on Calculator and all existing descendant HWNDs, not only the top-level frame.
PASS (static): Calculator clicks retain their original mouse messages while Statistics is foreground/active, so control commands remain usable without transferring activation.
PASS (static): opening Statistics explicitly calls SetForegroundWindow and SetActiveWindow before wx focus assignment, making the activation predicate reliable.
PASS (static): z-order reinforcement uses SetWindowPos(HWND_TOP, SWP_NOACTIVATE) only for the owned Statistics window and therefore keeps it above Calculator without making it globally topmost.
PASS (static): closing Statistics removes the owner and descendant subclasses, avoiding stale companion HWND references before a later reopen.
PASS (static): RET remains the explicit path that raises/focuses Calculator; calculation, History, decimal-separator, layout, and button-painting code are unchanged.
PASS: source ZIP integrity, complete SHA-256 source manifest, buildfix54 -> buildfix55 patch, and fresh-extraction verification were rerun.

Runtime/toolchain limitation remains: cargo/rustc/rustfmt and a Windows runtime are unavailable in this environment, so the native ownership, z-order, and activation behavior could not be live-tested here.


Buildfix56 bundled Help-file checks
-----------------------------------
PASS: the supplied `hlp-viewer(1).exe` is packaged as `hlp-viewer.exe` beside the project-root/final OpenCalc executable.
PASS: the supplied `CALC(1).CNT` is packaged as `CALC.CNT`; its `:Base CALC.HLP` directive matches the canonical packaged HLP name.
PASS: the uploaded HLP file (source upload name `CALC(4).HLP`) is packaged as `CALC.HLP`, which is the name OpenCalc passes to the viewer.
PASS: build.bat copies `hlp-viewer.exe`, `CALC.HLP`, and `CALC.CNT` into `target\release` beside Cargo's `OpenCalc.exe`; the project-root copies remain beside the published root `OpenCalc.exe`.
PASS: supplied-file SHA-256 values are recorded below and match the packaged copies:
  3400515485659278199f021c448a9ee418eaf54531665219fc1d989343f8e55d  hlp-viewer.exe
  4a81043c85ecbf495acbaa04ca4373889ee7d6c6536942b0b9ca4770ef01f1b7  CALC.CNT
  258f46db36f1e26b4c51784288c0a1253b17a39f70d6c79e58b3cdd85dc3d9eb  CALC.HLP
PASS: source ZIP integrity, the complete source manifest, buildfix55 -> buildfix56 binary patch, and fresh-extraction verification were rerun.

Runtime/toolchain limitation remains: cargo/rustc/rustfmt and a Windows runtime are unavailable in this environment, so the final viewer launch and CNT hierarchy could not be live-tested here.


Buildfix57 one-time Statistics centering checks
-------------------------------------------------
PASS (static): the positioning code is isolated in `center_stats_box()` and is invoked only when `open_stats_box()` has just constructed a fresh Statistics window.
PASS (static): closing Statistics still drops `ui.stats_box`, so reopening constructs and centers a new instance relative to Calculator.
PASS (static): Calculator activation no longer invokes Statistics positioning; it only updates application-group activation/z-order state.
PASS (static): Calculator move and size event handlers no longer recenter or otherwise reposition Statistics.
PASS (static): mode changes, History visibility/width changes, sash normalization, undo/redo restoration, and other Calculator layout synchronization no longer invoke Statistics positioning.
PASS (static): minimize/restore uses `show_open_stats_box()`, which restores visibility without changing the existing Statistics coordinates.
PASS (static): buildfix55 native ownership, descendant mouse-activation guards, explicit opening activation, RET focus transfer, and dataset persistence are unchanged.
PASS: source ZIP integrity, complete SHA-256 source manifest, buildfix56 -> buildfix57 binary patch, and fresh-extraction verification were rerun.

Runtime/toolchain limitation remains: cargo/rustc/rustfmt and a Windows runtime are unavailable in this environment, so the final native positioning and activation behavior could not be live-tested here.


Buildfix58 optional graph pane checks
-------------------------------------
PASS (static): `Settings` defaults `graph_visible` to false, parses the optional CFG key, writes it to `OpenCalc.cfg`, and preserves false for legacy configuration files where the key is absent.
PASS (static): View contains an independently checked Graph item; toggling it persists the preference and resizes the shared root surface without altering the Calculator pane's recovered Standard/Scientific dimensions.
PASS (static): the graph panel is a native left-side child containing the function field, Plot button, rectangular buffered canvas, visible-range roots line, Reset view button, and Export button below the canvas.
PASS (static): graph expressions reuse the existing lexer/parser through a reusable compiled token stream with graph-only variable `x`; ordinary calculator expression evaluation still rejects `x`.
PASS (static): bare expressions, `y=...`, `f(x)=...`, and one two-sided equation are normalized into one graphable function.
PASS (static): sampling rejects non-finite values, splits excessive vertical jumps, performs automatic y-ranging, and refreshes roots after plotting, zooming, panning, resetting, decimal-separator changes, and angle-mode changes.
PASS (static): roots are searched only in the visible x-range, crossing roots are bracketed/refined, tangent roots receive a separate local-minimum/Newton refinement pass, candidates are re-evaluated near zero, and duplicate candidates are merged.
PASS (static): root values and graph labels follow the active period/comma preference; root points and compact labels are drawn on the x-axis.
PASS (static): mouse dragging pans, the wheel performs pointer-centred zooming, Reset view restores the default domain, and Enter in the expression field invokes Plot.
PASS (static): Export offers PNG, JPG/JPEG, and SVG; missing extensions are supplied from the selected filter, and the current canvas size and viewport are used for output.
PASS (static): graph UI labels and statuses are available in English, Portuguese, and Spanish.
PASS (static): UTF-8 decoding, balanced-delimiter/source scans, source manifest verification, clean buildfix57 -> buildfix58 patch application, and fresh ZIP extraction verification were rerun.

Runtime/toolchain limitation remains: cargo/rustc/rustfmt and native Windows/Linux GUI runtimes are unavailable in this environment, so compilation, interactive plot rendering, file-dialog export, and live mouse navigation could not be executed here.


Buildfix59 concise-label checks
--------------------------------
PASS (static): the History action label is `Clear` in English, `Limpar` in Portuguese, and `Borrar` in Spanish.
PASS (static): the graph action label is `Export` in English and `Exportar` in both Portuguese and Spanish; no ellipsis remains on the button.
PASS (static): the existing `clear_history()` and graph-export event wiring is unchanged, so only localized display text changed.
PASS (static): the graph export file dialog still opens and still offers PNG, JPG/JPEG, and SVG.
PASS (static): button coordinates, sizes, DPI behavior, History layout, Graph layout, and localization refresh paths are unchanged.
PASS (static): UTF-8 decoding, source-manifest verification, clean buildfix58 -> buildfix59 patch application, and fresh ZIP extraction verification were rerun.

Runtime/toolchain limitation remains: cargo/rustc/rustfmt and native Windows/Linux GUI runtimes are unavailable in this environment, so the final rendered labels could not be live-tested here.


Buildfix60 presentation-export and localized-Help checks
-------------------------------------------------------
PASS (static): PNG, JPG/JPEG, and SVG exports now share one presentation renderer with a separate OpenCalc/function header, central graph, localized roots footer, and visible x/y range.
PASS (static): common function notation is formatted without changing evaluation: bare expressions receive `f(x) =`, integer powers use superscript digits, multiplication uses a centered dot, subtraction uses a mathematical minus, `pi` becomes `π`, and `sqrt(` becomes `√(`. Fractional exponents remain in caret notation rather than mixing baseline and superscript digits.
PASS (static): exported root markers no longer carry overlapping numeric labels inside the chart; the complete localized root sentence is wrapped into a bounded footer of at most three lines.
PASS (static): raster export uses a minimum 960x620 presentation surface; SVG uses the same page composition and retained vector primitives.
PASS (static): Help Topics/F1 reads the currently selected interface language when invoked. English prefers `CALC.HLP`, Portuguese prefers `CALP.HLP`, and Spanish prefers `CALS.HLP`; localized searches retain English as a compatibility fallback.
PASS (static): `build.bat` copies `hlp-viewer.exe`, `CALC.HLP`/`CALC.CNT`, `CALP.HLP`/`CALP.CNT`, `CALS.HLP`/`CALS.CNT`, and `calc.tooltip` into `target\release` beside `OpenCalc.exe`.
PASS: the retained English files are byte-identical to the current supplied pair:
  258f46db36f1e26b4c51784288c0a1253b17a39f70d6c79e58b3cdd85dc3d9eb  CALC.HLP
  4a81043c85ecbf495acbaa04ca4373889ee7d6c6536942b0b9ca4770ef01f1b7  CALC.CNT
PASS: Portuguese Help validates as 401 linked TOPIC/TOPICLINK records, 41 topic headers, 41 CNT destinations, preserved English symbolic topic-header positions, localized SYSTEM title, and internal CNT name `CALP.CNT`:
  f0831a45f19ef8357740fbe75f84665d18364a26a0e1f90191bb0c2cc02ec8d6  CALP.HLP
  242204a769ec47ef196d06c8789325926e7a7d0724a452bfb4b4c81b004ea9ad  CALP.CNT
PASS: Spanish Help validates as 401 linked TOPIC/TOPICLINK records, 41 topic headers, 41 CNT destinations, preserved English symbolic topic-header positions, localized SYSTEM title, and internal CNT name `CALS.CNT`:
  3eb25121089f8d144dc1530c6582cc20adba657ce4d3f558e589e20d18e09196  CALS.HLP
  f98286d253f4ccbec706ef1f5bb3ea48974b24358e51a3f0ca5ced3092bdc0be  CALS.CNT
PASS: both localized manuals retain the complete copy/paste reference topics and explicitly document `+ - * / ^ ** % ! mod and or xor lsh root`, unary signs, nested parentheses, trailing equals signs, functions/constants, locale-tolerant decimals, scientific notation, and `0x`/`0o`/`0b` literals.
PASS: localized CNT files are stored in the Windows-1252-compatible encoding expected by the legacy WinHelp ecosystem, not UTF-8.
PASS (static): all Rust files decode as UTF-8 and pass the comment/string-aware balanced-delimiter scan. Help routing call sites and graph-export call sites use the new language/root-summary arguments consistently.

Runtime/toolchain limitation remains: cargo, rustc, rustfmt, native Windows/Linux GUI runtimes, and WinHelp runtime execution are unavailable in this environment. No local Rust compilation, live HLP navigation, or rendered export smoke test is claimed.


Buildfix61 manual syntax audit
--------------------------------
PASS: English CALC.HLP, Portuguese CALP.HLP, and Spanish CALS.HLP retain 401 linked TOPIC records, 41 topics, unchanged symbolic topic positions, and matching 41-destination CNT hierarchies.
PASS: all three manuals document the renderable multiplication sign × and division sign ÷ aliases. The Unicode-minus parser alias is deliberately not printed in the legacy HLP manuals; visible Help uses ASCII `-`.
PASS: all three manuals state that ASCII function, constant, operator, and based-prefix names are case-insensitive and give uppercase/mixed-case examples.
PASS: all three manuals document the parser's exact precedence order: postfix !/%, unary signs, right-associative powers/root, lsh, multiplication/division/mod, addition/subtraction, and the shared left-associative and/xor/or level.
PASS: the unusual but current unary-before-power result (-2^2 = 4) and the parenthesized alternative (-(2^2) = -4) are stated explicitly.
PASS: leading-decimal .5 and ,5 forms are documented together with case-insensitive 0x/0X, 0o/0O, and 0b/0B literals.
PASS: the graph-field reference documents x, bare expressions, y = expression, f(x) = expression, general equations normalized as left-minus-right, and the one-function/one-equation restriction.
PASS: the manuals distinguish graph-only x from ordinary Calculator paste.
PASS: CALC.CNT, CALP.CNT, and CALS.CNT use localized Paste and Graph Examples titles while retaining the existing paste_examples context target.
PASS: HLP byte size, record linkage, topic count, topic starting offsets, CNT targets, UTF-8 source scans, source manifest, archive integrity, and buildfix60 -> buildfix61 binary patch reproduction were validated.

Runtime/toolchain limitation is unchanged: cargo, rustc, rustfmt, Wine, and a Windows GUI runtime are unavailable, so no compiled viewer launch is claimed.

Buildfix62 Help-directory / TOPICLINK repair
--------------------------------------------
PASS: reproduced the buildfix61 Portuguese failure structurally: CALP.HLP contains a TOPICLINK beginning at TOPICPOS 8162, offset 4066 of a 4084-byte transformed block, so its fixed 21-byte header crosses the block boundary.
PASS: buildfix62 does not weaken the viewer. All three manuals were repacked so every TOPICLINK header fits wholly inside one transformed block; payload data remains free to cross block boundaries.
PASS: after repacking, BlockSize, DataLen2, Previous, and Next fields were rewritten from the final records, and every physical TOPICBLOCKHEADER was regenerated from the final chain (LastTopicLink, FirstTopicLink, LastTopicHeader).
PASS: English Help validates as 401 linked TOPICLINK records, 41 topic headers, zero crossing headers, coherent TOPICBLOCKHEADER metadata, and SYSTEM contents file CALC_EN.CNT.
PASS: Portuguese Help validates as 401 linked TOPICLINK records, 41 topic headers, zero crossing headers, coherent TOPICBLOCKHEADER metadata, and SYSTEM contents file CALC_PT-BR.CNT.
PASS: Spanish Help validates as 401 linked TOPICLINK records, 41 topic headers, zero crossing headers, coherent TOPICBLOCKHEADER metadata, and SYSTEM contents file CALC_ES.CNT.
PASS: all three CNT files retain 41 authored destination rows and use an exact :Base matching their HLP filename.
PASS: the English manual's visible text is unchanged from buildfix61 except for the six Help-file/path documentation slots required by the new layout.
PASS: Unicode aliases, case-insensitive names, exact precedence, leading-decimal notation, and graph-field syntax remain documented in English, Portuguese, and Spanish.
PASS: project-root CALC.HLP/CALC.CNT, CALP.HLP/CALP.CNT, and CALS.HLP/CALS.CNT are removed. The runtime manuals now live only under Help/ as:
  Help/CALC_EN.HLP
  Help/CALC_EN.CNT
  Help/CALC_PT-BR.HLP
  Help/CALC_PT-BR.CNT
  Help/CALC_ES.HLP
  Help/CALC_ES.CNT
PASS: Help Topics/F1 selects CALC_EN.HLP for English, CALC_PT-BR.HLP for Portuguese, and CALC_ES.HLP for Spanish from the adjacent Help directory. Portuguese and Spanish retain CALC_EN.HLP as fallback.
PASS: build.bat creates target\release\Help, removes obsolete buildfix60/61 root-style Help filenames from target\release, and copies all six current files into target\release\Help.
PASS: localized missing-Help diagnostics now direct the user to the Help directory.
PASS: static Rust delimiter/source scan passes for every src/*.rs file.

Localized Help SHA-256:
  e04c0f73e7ba237c9239c050e23e48a1edba183d9f63f66f64546efcb717b3a7  Help/CALC_EN.HLP
  62ce38bc767a27a8e32da28dec7aca4c9046964456d793ea0f19e22badf3add2  Help/CALC_EN.CNT
  d73968038711a271a40515e17db83ff275bbc23e840dad05a39071a2cf74f7e6  Help/CALC_PT-BR.HLP
  704c2bf3e4eca1e3c3dac07fdba559205c1fe5ee60d61da0f8cdf46be75b60ba  Help/CALC_PT-BR.CNT
  ff2b2c9d730ac3cbbd81666587cf4a799037d4b3af5ca2cbb5f6b154bab52d4b  Help/CALC_ES.HLP
  c62df516bd06462e71b5f4a232e1e8a921b87c47d6f3025d34da7235ff4bb609  Help/CALC_ES.CNT

Runtime/toolchain limitation
----------------------------
This environment does not contain cargo, rustc, rustfmt, Wine, or a native Windows GUI runtime. No local Rust compilation or live HLP-viewer launch is claimed. The block-layout validation above mirrors the viewer's strict 21-byte TOPICLINK-header invariant and checks the complete 401-record chain for each manual.


Buildfix63-64 intermediate manual cleanup
------------------------------------------
PASS (historical): these builds introduced the precedence-example organization and the Portuguese `Copiar e colar expressões` wording. Buildfix66 supersedes their compiled HLP payloads with the format-aware Help revision validated below.

Buildfix65 focused-keyboard / double-star audit
-----------------------------------------------
PASS (binary audit): recovered CALC.EXE SHA-256 remains b064b0ac430264eff7b79b91e743bcd36d7b3707857f5bcdc4db146911dd0e28.
PASS (binary audit): CALC.EXE loads accelerator resource `SA` with LoadAcceleratorsA at 0x004012E9.
PASS (binary audit): when the Statistics dialog exists, IsDialogMessageA at 0x00401321 gets first chance; otherwise/unconsumed messages reach TranslateAcceleratorA at 0x0040133B before TranslateMessage/DispatchMessageA at 0x00401345..0x0040134B.
PASS (binary audit): the `SA` accelerator table contains the original digit/hex, arithmetic, decimal, Enter/editing, scientific-letter, memory/statistics Ctrl, radix/angle F-key, and Ctrl/Shift+Insert clipboard mappings documented in REVERSE_ENGINEERING.md.
PASS (static): OpenCalc installs one shared character handler on the Calculator frame/host, Standard and Scientific panels, Inv/Hyp, radix radios, and angle radios; the graph expression TextCtrl is intentionally excluded.
PASS (static): action-button clicks restore focus to the Calculator frame so subsequent keystrokes continue to operate the Calculator.
PASS (static): focused keyboard input routes through the same Action/Calculator methods as native control clicks rather than maintaining a second calculation engine.
PASS (static): ASCII `*` uses the new KeyboardStar action; a second consecutive star replaces pending multiplication with exponentiation. Numeric-keypad `*` and Unicode `×` remain multiplication.
PASS (source regression added): `keyboard_double_star_is_exponentiation_in_both_modes` checks 2**3 == 8 in Standard and Scientific modes.
PASS (compatibility): Scientific `^` remains the recovered Xor shortcut; `y` remains the recovered x^y shortcut.
PASS (compatibility): Alt/menu navigation and modern Ctrl+C/V/Z/Y menu accelerators are left to wxWidgets; Ctrl+Insert/Shift+Insert retain the recovered Copy/Paste aliases.

Runtime/toolchain limitation
----------------------------
This environment does not contain cargo, rustc, rustfmt, Wine, or a native Windows GUI runtime. The original PE/resource/message-loop audit and static source checks are complete, but the newly added Rust regression and live wxMSW keyboard interaction could not be executed locally.


Buildfix66 blank graph / format-aware Help rebuild
--------------------------------------------------
PASS (source): GraphModel::default() uses an empty input string, no compiled function, and RootResult::NotPlotted.
PASS (source): the graph TextCtrl is created with an empty value; the former startup `x^2 - 4` function is no longer pre-filled.
PASS (source regression added): `graph_starts_blank_and_unplotted` protects the blank default state.
PASS (packaging): the only runtime manual pairs are Help/CALC_EN.HLP/CNT, Help/CALC_PT-BR.HLP/CNT, and Help/CALC_ES.HLP/CNT; build.bat copies those exact files into target\release\Help.
PASS (localization): Help/CALC_PT-BR.CNT uses the top-level chapter `Copiar e colar expressões`, and the Portuguese HLP quick-start cross-reference uses the same wording.
PASS (visible Help): no packaged HLP contains the literal code-point label previously used for Unicode minus. The manuals retain × and ÷ aliases and use ASCII `-` for minus.
PASS (formatting): each HLP |FONT stream contains seven descriptors, adding a bold Segoe UI body face and a bold Consolas code face to the existing body/title/heading/code/caption set.
PASS (formatting): ordinary body references to Standard/Scientific mode and their Portuguese/Spanish equivalents are emitted with actual WinHelp 0x80 bold-font runs; mathematical expressions receive bold body or bold monospace runs as appropriate.
PASS (real table): each manual contains exactly one top-level type-0x23 WinHelp table in the paste-precedence topic. It has two columns, localized Expression/Result headings, and four rows: `2^3!`→`64`, `2^3^2`→`512`, `-2^2`→`4`, and `-(2^2)`→`-4`. The old pipe-delimited pseudo-table rows are gone.
PASS (table structure): all ten nested cells are bounded modern type-0x20 compact display records; header cells select bold-body font 5 and value cells select bold-code font 6.
PASS (keyboard docs): all three Keyboard Shortcuts topics state that focused Calculator keys operate calculator actions, summarize the classic one-letter scientific keys, document Ctrl+Insert/Shift+Insert, and identify `**` as exponentiation.
PASS (TOPIC integrity): all three manuals retain 401 top-level TOPICLINK records and 41 topic headers; exactly one record changes from display to table. Every fixed 21-byte TOPICLINK header remains wholly inside its 4,084-byte transformed block.
PASS (navigation stability): every top-level record remains in its original transformed block. The three precedence-example records preserve their combined TopicLength contribution (53), so subsequent TOPICOFFSET accounting and all later topic starts remain unchanged.

Buildfix66 runtime limitation
-----------------------------
This environment still does not contain cargo, rustc, rustfmt, Wine, or a native Windows GUI runtime. The HLP/CNT binaries were structurally decoded and validated after regeneration, but the final wxMSW executable and rendered Help/table appearance could not be launched locally.


Buildfix67 structured Help layout
---------------------------------
PASS: English, Portuguese, and Spanish packaged HLP files were regenerated from the buildfix66 manuals; build output continues to copy these exact files from Help/.
PASS: Pasted Expression Syntax contains two real type-0x23 WinHelp tables: a bordered operator-group table and a bordered highest-to-lowest precedence table. The old pipe-delimited pseudo-table text is absent.
PASS: the syntax introduction and unary/final-equals explanation use authored paragraph-border boxes.
PASS: Paste and Graph Examples contains a bordered example table, while the graph-field forms/limitations are grouped in a continuous bordered callout.
PASS: Keyboard Shortcuts groups the menu accelerator list in a box and renders the focused direct-key mapping as a bordered table.
PASS: Calculator Error Messages uses a bordered Message/Meaning table; Standard Button Reference uses a bordered Buttons/Action table.
PASS: Functions/Constants and Numbers/Locales use bordered callouts for dense grammar/reference paragraphs and example groups; the Scientific Button Reference matrix rows are boxed so the control layout reads as a distinct reference panel.
PASS: each localized manual decodes to 401 TOPICLINK records and 41 TopicHeader records; every 21-byte TOPICLINK header fits wholly within one transformed block.
PASS: all 41 TopicHeader TOPICPOS values are byte-for-byte unchanged from buildfix66. `|CONTEXT` and `|TopicId` streams are also byte-for-byte unchanged.
PASS: moved display records are relinked and the TopicHeader internal topic offsets are remapped to the new positions.
PASS: no `U+2212`, `Expr. | Res.`, `Tabela: Expr.`, or `Tabla: Expr.` fallback text remains in the packaged HLP payloads.
PASS: no Rust calculator behavior changed in buildfix67; this build changes Help presentation, README/VALIDATION notes, packaging hashes, and the source manifest only.


Buildfix68 top-level keyboard focus / button feedback
----------------------------------------------------
PASS (source): after the main frame is shown, fitted and centered, OpenCalc explicitly calls `frame.set_focus()`, so calculator keyboard input no longer depends on clicking CE or another pushbutton first.
PASS (source): when the Calculator top-level window becomes active again on Windows, the existing activation handler restores frame keyboard focus unless the graph expression TextCtrl deliberately owns focus.
PASS (compatibility): graph expression editing remains exempt from Calculator key routing; typed functions/expressions continue to enter the graph field normally.
PASS (source): all keyboard-triggered calculator Actions now pass through `perform_from_keyboard`, which pulses the matching visible action button before performing the same calculation path used by a mouse click.
PASS (Windows painter): `pulse_classic_button` sends `BM_SETSTATE`, invalidates the button, and uses an 85 ms window timer; the existing classic painter reads `BM_GETSTATE/BST_PUSHED`, so the same DPI-aware pressed bevel used for mouse interaction is drawn for keyboard input.
PASS (repeat behavior): a repeated key kills/re-arms the same per-button timer rather than blocking the UI thread or creating an unbounded timer queue.
PASS (mode scoping): button lookup is restricted to the currently visible Standard or Scientific action-button group so duplicate actions such as CE, C, digits and arithmetic pulse the visible control, not the hidden mode's copy.
PASS (compatibility): `KeyboardStar` animates the multiplication key while retaining buildfix65's `**` exponentiation semantics; selectors/F-key changes continue to provide their own native state change rather than pretending to be pushbuttons.

Runtime/toolchain limitation
----------------------------
This environment still does not contain cargo, rustc, rustfmt, Wine, or a native Windows GUI runtime. Static source/package verification was performed, but the final wxMSW focus transition and visible key-press animation must be confirmed on Windows.


Buildfix69 two-column keyboard reference
---------------------------------------
PASS: topic 32 in CALC_EN.HLP contains a real two-column type-0x23 table headed `Key` / `Shortcut`.
PASS: topic 32 in CALC_PT-BR.HLP contains a real two-column type-0x23 table headed `Tecla` / `Atalho`.
PASS: topic 32 in CALC_ES.HLP contains a real two-column type-0x23 table headed `Tecla` / `Atajo`.
PASS: key/group spellings occupy the left cells and the corresponding calculator actions occupy the right cells; the former one-column `Key groups` / `Grupos de teclas` presentation is gone.
PASS: all three manuals retain 401 TOPICLINK records, 41 topics, zero crossing TOPICLINK headers, and the exact same 41 TopicHeader positions as buildfix68.
PASS: no `U+2212` text reappears.
PASS: no Rust calculator behavior changed in this documentation-only build.


Buildfix70 complete Help replacement
------------------------------------
PASS: the project Help directory was deleted and recreated from the current buildfix69 Help package; only the six canonical localized HLP/CNT files remain.
PASS: build.bat now recursively deletes target\release\Help before recreating it, so stale manuals from previous builds cannot survive release packaging.
PASS: English, Portuguese, and Spanish HLP/CNT files are all copied anew into target\release\Help.
PASS: no calculator/runtime logic changed in buildfix70.

Buildfix71 one-entry-per-row Help lookup tables
------------------------------------------------
PASS: the English pasted-expression syntax topic contains a native two-column `Operator` / `Description` table with one row per operator or delimiter.
PASS: the Portuguese and Spanish equivalents use `Operador` / `Descrição` and `Operador` / `Descripción`.
PASS: `!` has a dedicated factorial row explaining that a nonnegative integer is multiplied by every lower positive integer; it is no longer hidden inside a grouped punctuation row.
PASS: `**` has its own exponentiation row, separate from ordinary `*` multiplication.
PASS: the direct-key reference is split into three native two-column lookup tables: basic/editing keys, scientific keys, and function/control shortcuts. Each key or genuinely equivalent key pair has its own row and a full description.
PASS: grouped prose rows such as `0-9 / A-F - number / hex entry`, the `Key groups` heading, and the one-cell shortcut summaries are absent from all three HLP payloads.
PASS: CALC_EN.HLP, CALC_PT-BR.HLP, and CALC_ES.HLP each decode to 398 linked TOPICLINK records, 41 TopicHeader records, eight native tables, and 13 physical `|TOPIC` blocks; no 21-byte TOPICLINK header crosses a block boundary.
PASS: all 41 `|CONTEXT` hashes and all 41 `|TopicId` names resolve to the same topic titles as buildfix70; browse navigation resolves to the same destination titles.
PASS: the localized CNT files remain unchanged and continue to point to their matching HLP filenames.
PASS: `tools/rebuild_help_reference_tables.py` is idempotent on the rebuilt manuals and reproduces the format-aware migration from the buildfix70 Help set.
PASS: no Rust calculator/runtime behavior changed in buildfix71.

Buildfix72 graph parser and status layout
-----------------------------------------
PASS (source review): graph normalization converts `2x² + 2x + 2` to `2*x^2+2*x+2` before the shared expression parser runs.
PASS (source tests added): the graph model accepts equivalent quadratic expressions using Unicode `²`, caret `^`, and double-star `**` notation.
PASS (source tests added): graph-only implicit multiplication covers `2x`, `2(x+1)`, `(x+1)(x-1)`, and `2sqrt(x)` without changing ordinary Calculator paste syntax.
PASS (source review): failed normalization or parsing clears the previous compiled graph, stores the new input/error, and prevents a stale curve or caption from remaining visible.
PASS (source review): the graph status area is increased from one line to three display lines, wraps at a conservative width, and reapplies the fixed graph-pane layout after SetLabel.
PASS: all three HLP manuals document `2x² + 2x + 2`, `2x^2 + 2x + 2`, and `2x**2 + 2x + 2` as equivalent graph-field forms.
PASS: regenerated English, Portuguese, and Spanish HLP files parse as 398 TOPICLINK records and 41 topics with zero crossing 21-byte headers.
LIMITATION: cargo, rustc, rustfmt, Wine, and a native Windows GUI runtime are unavailable here. The Rust tests and final wxMSW rendering could not be executed locally.


Buildfix73 help-table alignment cleanup
--------------------------------------
PASS: English, Portuguese, and Spanish operator/keyboard tables now use shorter one-line descriptions to prevent wrapped cells from visually desynchronizing later rows.
PASS: the packaged CALC_EN.HLP, CALC_PT-BR.HLP, and CALC_ES.HLP files were replaced with updated binaries containing the shortened table text.
PASS: tools/rebuild_help_reference_tables.py now reserves more width for the description column in rebuilt operator, basic, scientific, and control tables.
PASS: no Rust calculator behavior changed in this build; the change is limited to packaged Help content, the help-table rebuild script, README notes, VALIDATION notes, and the source manifest.


Buildfix74 padded Help tables
-----------------------------
PASS: CALC_EN.HLP, CALC_PT-BR.HLP, and CALC_ES.HLP were regenerated with shorter operator-description text to avoid wrapped rows in the viewer.
PASS: tools/rebuild_help_reference_tables.py now reserves a 75/675 operator table split and 95/645 key table split so the left column stays compact and the description column remains padded.
PASS: packaged help files were replaced in full, not incrementally overlaid.
PASS: no calculator runtime code changed in this build.


Buildfix75 aligned keyboard tables
----------------------------------
PASS: CALC_EN.HLP, CALC_PT-BR.HLP, and CALC_ES.HLP were fully repacked with newly generated operator, basic-key, scientific-key, and control-shortcut tables.
PASS: each table cell now has explicit nonbreaking-space padding.
PASS: keyboard descriptions are capped at 40 characters or fewer, preventing the legacy viewer from wrapping one cell while leaving its paired key cell at single-line height.
PASS: equivalent keys such as Enter and =, Backspace and Left, and period and comma now occupy separate rows.
PASS: operator table geometry is 88/652; basic-key geometry is 125/615; scientific-key geometry is 100/640; control-shortcut geometry is 130/610.
PASS: each regenerated manual has 398 TOPICLINK records, 41 topics, and zero TOPICLINK headers crossing transformed-block boundaries.
PASS: no calculator runtime code changed in this build.


Buildfix76 row-safe Help tables
-------------------------------
PASS: English, Portuguese, and Spanish Help files were regenerated from buildfix75 with one two-cell type-0x23 record for every visible operator/key table row.
PASS: each rebuilt manual contains 477 TOPICLINK records, 41 topics, 14 physical |TOPIC blocks, and 83 generated two-cell row-table records.
PASS: every generated row-table record contains exactly two nested display cells.
PASS: row heights are isolated per record, so wrapping in a key or description cannot shift the following rows in the other column.
PASS: nonbreaking-space padding remains inside each cell paragraph; no padding-only paragraph records are emitted.
PASS: no TOPICLINK header crosses a transformed 4,084-byte data-block boundary.
PASS: |CONTEXT, |TopicId, |SYSTEM contents offsets, TOPICPOS links, and TOPICOFFSET links are translated through the rebuilt stream.
PASS: tools/repad_help_tables.py now delegates command-line regeneration to the row-safe generator.
PASS: no calculator runtime code changed in this build.

Buildfix77 Linux-only UI, sizing, modal-dialog, and script checks
----------------------------------------------------------------
PASS: all buildfix77 palette, font, splitter-decoration, Statistics-focus, modal-dialog, and GTK sizing behavior now resides in src/ui/linux.rs or src/platform/linux.rs, selected only for target_os = "linux".
PASS: Windows continues to use Microsoft Sans Serif, its native button-face colour and control painters, platform::message/MessageBoxW, the existing Statistics foreground-activation path, the splitter-boundary overlay, and the native realized-HWND sizing helper.
PASS: buildfix79 supersedes the earlier warm buildfix77 face: Linux calculator surfaces and GTK button/field CSS now use exact RGB(240,240,240) / #f0f0f0, while Linux continues to request Liberation Sans.
PASS: on Linux the decorative History separator is a child of the History pane at x=0 instead of a child over the wxSplitterWindow sash, leaving the native sash available for drag input.
PASS: Linux no longer applies asynchronous equal min/max geometry hints after every layout; gtk_window_set_resizable(false) prevents user resizing while the explicit surface/client sizing and layout/update pass preserve programmatic Standard, Scientific, Graph, and History size changes.
PASS: Graph visibility remains mode-independent and sync_mode_surface includes GRAPH_W for both Standard and Scientific modes; Linux additionally falls back to logical set_size_with_pos rectangles for the Graph panel and Calculator/History splitter when the Win32-only pixel helper returns false.
PASS: opening Statistics on Linux raises the utility window but restores keyboard focus to Calculator; the Windows companion activation/focus implementation is isolated in src/ui/windows.rs and unchanged in behavior.
PASS: Linux About, Help failures, settings errors, clipboard errors, and graph-export errors use a parent-owned synchronous wxDragon MessageDialog in src/ui/linux.rs. Windows routes the same shared requests through its native platform::message implementation.
PASS: Linux Help discovery requires an executable extensionless hlp-viewer beside OpenCalc; a Linux-only regression test covers missing and executable permission states.
PASS: the supplied build.sh is incorporated, packages OpenCalc/runtime files into build-linux, and intentionally excludes hlp-viewer.exe.
PASS: clean-linux.sh removes target, build-linux, a root OpenCalc test binary, and Zone.Identifier marker files without deleting source, Help files, or viewer inputs.
PASS: bash -n accepts build.sh and clean-linux.sh. Python helper sources compile successfully. UTF-8 and Rust delimiter scans pass.

Runtime/toolchain limitation
----------------------------
This artifact environment does not contain cargo, rustc, or a wxGTK display, so no local Rust compilation or live Linux GUI smoke test is claimed. The source changes were statically inspected against wxDragon 0.9.17 APIs, and the scripts were syntax-checked. Run ./build.sh in the prepared WSL toolchain for the authoritative compile and UI test.



Buildfix79 Linux clipboard and neutral-face checks
-------------------------------------------------
PASS (source review): Linux copy_text uses the GTK3 default clipboard, publishes UTF-8 display text, and asks the desktop clipboard manager to retain the copied value.
PASS (source review): Linux paste_text synchronously requests UTF-8 text, returns None for an empty/non-text clipboard, converts the GLib buffer before releasing it with g_free, and then uses the existing shared paste-expression path.
PASS: the recovered Cannot open Clipboard. string is used when no GTK display or default clipboard is available.
PASS: the Linux wx surface colour is exactly RGB(240,240,240), and Linux GTK button/field faces are exactly #f0f0f0.
PASS: the former warm #d4d0c8 and #ece9d8 shades are absent from src/ui/linux.rs and src/platform/linux.rs.
PASS: all buildfix79 runtime changes remain confined to Linux backend files; Windows clipboard and frontend source files are byte-for-byte unchanged from buildfix78.
PASS: tools/check_platform_separation.py confirms matching backend facades and target-neutral shared modules; tools/check_rust_delimiters.py accepts all 22 Rust source files.
PASS: build.sh and clean-linux.sh pass shell syntax checks, and clean-linux.sh passes an isolated removal/preservation test.
PASS: the required GTK3, GDK3, and GLib clipboard/display/free symbols are present in the Linux runtime libraries available to this validation environment.
LIMITATION: cargo, rustc, and a wxGTK display are unavailable in this artifact environment, so the authoritative WSL compilation and live clipboard/UI smoke test must be run with ./build.sh.

Buildfix80 Linux layout validation
----------------------------------
PASS: Linux programmatic mode/pane transitions temporarily re-enable GTK resizing and restore the non-resizable policy after the exact client allocation is applied.
PASS: wxGTK child panes and the nested History splitter are laid out before the top-level frame is shrunk, preventing the previous Scientific allocation from leaving blank space in Standard mode.
PASS: Graph/History show-hide transitions use the same complete surface recalculation path as Standard/Scientific transitions.
PASS: History remains a native draggable wxSplitterWindow pane; Graph gains a Linux-only captured separator drag handle with bounded width.
PASS: Windows frontend sizing order, fixed Graph width, DPI fitter, palette, and native splitter policy are unchanged.
PASS: tools/check_platform_separation.py accepts matching UI/platform/locale backend facades.
PASS: tools/check_rust_delimiters.py accepts all Rust source files.
NOT RUN HERE: authoritative Cargo compilation and live wxGTK interaction test; this execution environment has no Rust toolchain or GTK/wxGTK development packages.

Buildfix81 focus-aware Graph clipboard validation
-------------------------------------------------
PASS (source review): the shared Copy/Paste command checks the native Graph Function editor's keyboard focus before invoking the numeric calculator clipboard path.
PASS (source review): focused Function-field Paste replaces the active selection or inserts at the caret and returns without numeric evaluation or Calculation History insertion.
PASS (source review): focused Function-field Copy exports only the selected editor text and is a no-op when there is no selection, matching native single-line editor behavior.
PASS: Linux implements the editor bridge through GTK3 GtkEditable selection, deletion, insertion, and caret APIs; buildfix82 now supplies the equivalent wxMSW bridge, while the portable backend retains its no-op contract fallback.
PASS: Graph normalization regression coverage includes `2x**2 + 3x + 10` -> `2*x**2+3*x+10`, and plotting regression coverage includes the same polynomial.
PASS: the numeric Calculator paste path remains unchanged whenever the Function editor does not own focus on either native frontend; a free variable is not silently assigned a value.
PASS: tools/check_platform_separation.py accepts matching UI/platform/locale backend facades and target-neutral shared modules.
PASS: tools/check_rust_delimiters.py accepts all Rust source files; build.sh and clean-linux.sh pass shell syntax checks.
NOT RUN HERE: authoritative Cargo compilation and live wxGTK/wxMSW clipboard interaction tests; this execution environment has no Rust toolchain or wxWidgets development environment.



Buildfix82 cross-platform Graph clipboard validation
----------------------------------------------------
PASS (source review): Windows editable_owns_clipboard now uses the same exact-child keyboard-focus rule as Linux, so frame-level Copy/Paste accelerators defer to the focused Graph Function field.
PASS (source review): the wxMSW editor bridge reads 32-bit selection positions with EM_GETSEL, slices the native UTF-16 window text on code-unit boundaries, and returns no text when the selection is empty.
PASS (source review): Windows Paste uses EM_REPLACESEL with undo enabled, replacing the active selection or inserting at the caret without evaluating the polynomial or adding a Calculation History entry.
PASS: Linux GtkEditable behavior remains unchanged, and the portable backend retains its no-op contract fallback.
PASS: shared UI logic contains no new target-specific API and continues to use the platform facade for both native frontends.
PASS: tools/check_platform_separation.py accepts matching UI/platform/locale backend facades and target-neutral shared modules.
PASS: tools/check_rust_delimiters.py accepts all Rust source files; build.sh and clean-linux.sh pass shell syntax checks.
NOT RUN HERE: authoritative Cargo compilation and live wxMSW/wxGTK clipboard interaction tests; this execution environment has no Rust toolchain or wxWidgets development environment.


Buildfix83 Linux Statistics transient/z-order validation
--------------------------------------------------------
PASS (source review): install_companion_activation_guard explicitly calls gtk_window_set_transient_for with Calculator as owner and marks Statistics as a utility omitted from the task switcher.
PASS (historical buildfix83 review): transient ownership is retained; buildfix84 supersedes the raise-only policy with an active-application keep-above guard that is removed when the complete OpenCalc window group becomes inactive.
PASS (source review): owner/utility synchronization raises the realized GDK window through gdk_window_raise, which changes z-order without requesting keyboard activation.
PASS (source review): the Statistics activation hook ignores deactivation instead of lowering the utility; this removes the owner-active/companion-inactive event-order race observed when Calculator is clicked.
PASS (source review): Linux focus_statistics returns keyboard focus to Calculator and then reasserts Statistics stacking without activating it.
PASS: the required gtk_window_set_transient_for, gtk_window_set_skip_taskbar_hint, gtk_widget_get_window, and gdk_window_raise symbols are present in the GTK3/GDK3 runtime libraries available to this validation environment.
PASS: Windows frontend/platform sources are byte-for-byte unchanged from buildfix82.
PASS: tools/check_platform_separation.py accepts matching backend facades and target-neutral shared modules.
PASS: tools/check_rust_delimiters.py accepts all Rust source files; build.sh and clean-linux.sh pass shell syntax checks.
NOT RUN HERE: authoritative Cargo compilation and live wxGTK window-manager interaction test; this execution environment has no Rust toolchain or wxWidgets/GTK development packages.

Buildfix84 Linux Statistics no-dip validation
---------------------------------------------
PASS (source review): install_companion_activation_guard enables gtk_window_set_keep_above before Statistics is shown, eliminating the interval in which a Calculator control click could raise the owner above the utility.
PASS (source review): active OpenCalc window notifications keep the above hint armed and reaffirm native GDK stacking without activating Statistics or stealing Calculator keyboard focus.
PASS (source review): deactivation is deferred with g_idle_add_full; the idle callback checks gtk_window_is_active for both Statistics and its gtk_window_get_transient_for owner, so focus transfers inside OpenCalc retain the hint.
PASS (source review): the retained GtkWindow is protected with g_object_ref/g_object_unref until the idle callback finishes; gtk_widget_in_destruction and gtk_widget_get_realized prevent a deferred restack from touching a closed native window.
PASS (source review): when neither OpenCalc top-level is active, the idle callback removes the keep-above hint, so unrelated applications can cover the complete OpenCalc group.
PASS: the required gtk_window_set_keep_above, gtk_window_get_transient_for, gtk_window_is_active, gtk_widget_in_destruction, gtk_widget_get_realized, g_idle_add_full, g_object_ref, and g_object_unref symbols are available in the GTK3/GLib/GObject runtime libraries used for static validation.
PASS: Windows frontend/platform sources remain byte-for-byte unchanged from buildfix83.
PASS: tools/check_platform_separation.py accepts matching backend facades and target-neutral shared modules.
PASS: tools/check_rust_delimiters.py accepts all Rust source files; build.sh and clean-linux.sh pass shell syntax checks.
NOT RUN HERE: authoritative Cargo compilation and live wxGTK window-manager interaction test; this execution environment has no Rust toolchain or wxWidgets/GTK development packages.



Buildfix85 Linux separator and group-frame validation
------------------------------------------------------
PASS (historical buildfix85 review): Linux introduced a splitter-level neutral History decoration that covered the desktop-themed warm strip. Buildfix86 retains the decoration but restores native sash input underneath it.
PASS (historical buildfix85 review): the temporary custom History drag handler has been superseded by buildfix86's pointer-transparent overlay and native wxSplitterWindow sash.
PASS (historical buildfix85 review): ThinSash was used underneath the custom handle; buildfix86 restores the normal Linux native sash while Windows and fallback styles remain unchanged.
PASS: Linux splitter and separator GTK styles use the exact neutral #f0f0f0 face, with the etched dark/light rule matching the Graph boundary.
PASS: Linux selector/command group rectangles now use one 1px etched border with no second inset black top/left band, making the upper edge the same thickness as the remaining edges.
PASS: Windows frontend and classic group-box painter behavior are unchanged.
PASS: tools/check_platform_separation.py accepts matching backend facades; tools/check_rust_delimiters.py accepts all Rust source files.
NOT RUN HERE: authoritative Cargo compilation and live wxGTK rendering/drag test; this execution environment has no Rust toolchain or wxWidgets/GTK development packages.


Buildfix86 native sash, Statistics input/toggle, and Graph focus validation
--------------------------------------------------------------------------
PASS (source review): Linux uses a normal native wxSplitterWindow sash again; the neutral etched History decoration is pointer-transparent and centered over the sash, while the shared sash-change handler persists the bounded History width.
PASS: gtk_widget_set_sensitive is available in the GTK3 runtime used for static validation; the Linux pointer-passthrough facade fixes decoration opacity at 1 and leaves the exact #f0f0f0 face unchanged.
PASS (source review): the Statistics frame, panel, and focusable list box all receive the shared Calculator character handler, so calculator keys are routed while Statistics is active on Windows and Linux.
PASS (source review): Sta removes the cached Statistics record before destroying the native frame, returns focus to Calculator, and creates a fresh centered frame on the next toggle without clearing Calculator.stats.
PASS (source review): Graph visibility no longer calls set_focus on the Function editor; the frame remains the keyboard target until the user directly focuses the editable field.
PASS (source review): Linux applies square classic display CSS to the editable Function field while Windows retains the native square wxMSW EDIT chrome and caret/selection painting.
PASS: tools/check_platform_separation.py accepts matching UI/platform/locale backend facades; tools/check_rust_delimiters.py accepts all Rust source files.
PASS: build.sh and clean-linux.sh pass shell syntax checks.
NOT RUN HERE: authoritative Cargo compilation and live wxMSW/wxGTK interaction tests; package installation for the local Rust/wxWidgets toolchain did not complete in this artifact environment.

Buildfix87 review:
PASS (static): Linux frontend selects the native History sash and adds a Linux-only leading gutter/sash extent; Windows and fallback policies return zero/false.
PASS (static): Linux GTK CSS keeps the sash surface at #f0f0f0, adds an 8px native hit target, and paints a centered dark/light etched rule.
PASS (static): shared sizing includes the Linux-only gutter and native sash extent while the recovered Standard/Scientific child panels retain their original dimensions.
PASS (static): History sash-change normalization uses the same platform-specific canonical Calculator-pane width as the layout path.

Buildfix88 Windows embedded-resource path validation
----------------------------------------------------
PASS: src/platform/windows.rs embeds the project-root calc95.ico with `include_bytes!("../../calc95.ico")`; from the refactored module directory this resolves to the existing `win95_calc_rust/calc95.ico`.
PASS: the stale `include_bytes!("../calc95.ico")` spelling is absent, so Windows no longer searches for the nonexistent `src/calc95.ico`.
PASS: tools/check_platform_separation.py now scans every Rust source file, including target-gated backends, and fails when a literal include_bytes!/include_str! path does not resolve relative to its source file.
PASS: platform-separation validation, Rust delimiter validation, Python syntax validation, Linux shell syntax checks, source-manifest verification, and ZIP integrity testing completed successfully.
NOT RUN HERE: authoritative wxMSW compilation and runtime smoke test; this environment does not provide a Windows Rust/wxWidgets toolchain.
